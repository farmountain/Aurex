[package]
name = "amduda"
version = "0.1.0"
edition = "2021"

[lib]
name = "amduda"
path = "src/lib.rs"

[dependencies]
aurex-runtime = { path = "../aurex-runtime" }
llvm-sys = "150"
once_cell = "1"

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread"] }
